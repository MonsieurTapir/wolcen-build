import { EimValueExtractorPipe } from './eim-value-extractor.pipe';

describe('EimValueExtractorPipe', () => {
  it('create an instance', () => {
    const pipe = new EimValueExtractorPipe();
    expect(pipe).toBeTruthy();
  });
});
