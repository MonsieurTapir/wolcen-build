import { EimFormatterPipe } from './eim-formatter.pipe';

describe('EimFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new EimFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
